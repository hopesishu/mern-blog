import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
    cardContent: {
        whiteSpace: "pre-wrap",
        height: "100px",
        overflow: "hidden",
        textOverflow: "ellipsis"
    },
}));